import {base} from './base';

export const environment = {
  ENV: 'production',
  FIREBASE_CONFIG: base.FIRE,
  FIREBASE_APPCHECK_DEBUG_TOKEN: base.DEBUG,
  CATPCHA: base.CATPCHA,
  VERSION: '0.0.1.1',
}
